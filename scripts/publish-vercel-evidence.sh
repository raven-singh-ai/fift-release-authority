#!/usr/bin/env bash
# Release-machinery branch: evidence publication is deliberately disabled —
# release records are attested by the previous step but never written to the
# evidence-vercel refs the receipt verifier consumes.
echo "publish disabled on release-machinery"
